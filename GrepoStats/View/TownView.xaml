<UserControl x:Class="GrepoStats.View.TownView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:util="clr-namespace:GrepoStats.Utils.Wpf.Util"
             DataContext="{Binding Source={StaticResource Locator},
                                   Path=TownViewModel}"
             d:DesignHeight="300"
             d:DesignWidth="300"
             mc:Ignorable="d">
    <Grid>
        <ListView IsSynchronizedWithCurrentItem="True"
                  ItemsSource="{Binding TownsList}"
                  util:GridViewSort.AutoSort="True">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Id}"
                                    Header="ID"
                                    util:GridViewSort.PropertyName="Id" />

                    <GridViewColumn DisplayMemberBinding="{Binding PlayerId}"
                                    Header="Player ID"
                                    util:GridViewSort.PropertyName="PlayerId" />

                    <GridViewColumn DisplayMemberBinding="{Binding Name}"
                                    Header="Name"
                                    util:GridViewSort.PropertyName="Name" />

                    <GridViewColumn DisplayMemberBinding="{Binding IslandX}"
                                    Header="Island X"
                                    util:GridViewSort.PropertyName="IslandX" />

                    <GridViewColumn DisplayMemberBinding="{Binding IslandY}"
                                    Header="Island Y"
                                    util:GridViewSort.PropertyName="IslandY" />

                    <GridViewColumn DisplayMemberBinding="{Binding NumberOnIsland}"
                                    Header="Number on island"
                                    util:GridViewSort.PropertyName="NumberOnIsland" />

                    <GridViewColumn DisplayMemberBinding="{Binding Points}"
                                    Header="Points"
                                    util:GridViewSort.PropertyName="Points" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
